public class Visualization {
    // Amount of collisions for each index at a specific mod value
    // The above averaged across n mod values.
    // The above for specific sets (even, odd, prime, composite)

    // Graph of time that it took for each calculation, averaged across the list.
}
